@model IEnumerable<Vieon.Models.Phim_DaoDien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VaiTro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DaoDien.TenDaoDien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phim.TenPhim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VaiTro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DaoDien.TenDaoDien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phim.TenPhim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Phim_DaoDien }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Phim_DaoDien }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Phim_DaoDien })
        </td>
    </tr>
}

</table>

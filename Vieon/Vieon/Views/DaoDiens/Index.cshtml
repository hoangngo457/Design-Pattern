@model IEnumerable<Vieon.Models.DaoDien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDaoDien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDaoDien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DaoDien }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DaoDien }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DaoDien })
        </td>
    </tr>
}

</table>
